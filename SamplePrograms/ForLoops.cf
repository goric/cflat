class forLoop
{
	void main()
	{
		int x; /* we'll initialize locals to their default values */

		for(int i = 0; i < 10; i++)
			x++;

		printf("{0} should be 10", x);

		for(a in [0..9])
			x--;

		print(x + " should be 0");
	}
}