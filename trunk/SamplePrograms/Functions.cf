class func
{
	void main()
	{
		int result = factorial(5);
		print(result);
	}

	int factorial(int n)
	{
		int fact = 1;
		int iterator = 1;

		while(iterator <= n)
		{
			fact = fact * iterator;
			iterator++;
		}
		
		return fact;
	}
	
	
}