class arrays
{
	void main()
	{
		int[] a = new int[10];

		for(i in [1..10])
		{
			a[i] = 0;
		}

		doSomething(readonly a);
	}

	private readonly void doSomething(int[] p)
	{
		p[0] = 6;
	}
}